<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  # Send all requests not pointing to a file or directory to index.html
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.html [L]
</IfModule>